"""susr package test suite.

Covers Layer 3 (domain invariants) and Layer 4 (engine units) of the
five-layer test taxonomy in CLAUDE.md §6.2.  Layer 1 + 2 lint/scenario
tests live at repo root (`tests/`) and are unaffected by this directory.

R1: scaffold only (conftest fixtures + README); the actual test files
are added in R2 alongside their implementations.
"""
